# Nom de l'application (gard� ici par convention, mais lu en priorit� depuis bootstrap.properties)
spring.application.name=microproject 

# --- Configuration Eureka Client (pour l'enregistrement POST-bootstrap) ---
# Ou pour tests locaux directs:
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true 
eureka.client.fetch-registry=true 
eureka.instance.preferIpAddress=true 


# ============================================================================

# ============================================================================

# Server Port
# server.port=8085 

# H2 Database Configuration
# spring.datasource.url=jdbc:h2:mem:courses
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=admin
# spring.datasource.password=admin

# JPA / Hibernate Configuration
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update 
# spring.jpa.show-sql=true 

# H2 Console Configuration
# spring.h2.console.enabled=true 
# spring.h2.console.path=/h2-console 

# Activation/D�sactivation Config/Discovery (Supprim�es car g�r�es par pr�sence des starters) # CORRIG�: Encodage