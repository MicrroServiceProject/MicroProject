<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            padding: 20px;
        }
        .header {
            color: #ffffff;
            background-color: #2980b9;
            padding: 15px;
            text-align: center;
            border-radius: 8px 8px 0 0;
            margin: -20px -20px 20px -20px;
        }
        .details {
            margin: 20px 0;
            padding: 15px;
            background-color: #ecf0f1;
            border-left: 4px solid #2980b9;
            border-radius: 4px;
        }
        .details p {
            margin: 10px 0;
            line-height: 1.5;
        }
        .details strong {
            color: #2c3e50;
        }
        .event-highlight {
            color: #e74c3c;
            font-weight: bold;
        }
        .footer {
            font-size: 0.8em;
            color: #7f8c8d;
            text-align: center;
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #ecf0f1;
        }
    </style>
</head>
<body>
<div class="container">
    <h2 class="header">Nouvelle Réservation #{{reservationId}}</h2>
    <div class="details">
        <p><strong>Client :</strong> {{clientName}} ({{clientEmail}})</p>
        <p><strong>Événement :</strong> <span class="event-highlight">{{eventName}}</span></p>
        <p><strong>Places :</strong> {{places}}</p>
        <p><strong>Date :</strong> {{reservationDate}}</p>
    </div>
    <div class="footer">
        Cet email a été envoyé automatiquement, merci de ne pas y répondre.
    </div>
</div>
</body>
</html>